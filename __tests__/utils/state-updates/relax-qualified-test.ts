import { relaxQualified } from '../../../lib/form/property';

describe('Unit tests on the Relax Qualified function', () => {
  it('Should not make any changes whenever there are no qualified fields', () => {
    relaxQualified;
  });
});
