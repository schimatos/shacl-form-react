export {};
// import type { sh } from '../types';

/**
 * Expands a single path into a nested shape
 * @param shape The shape to expand
 * @param recursive Whether or not to recursively apply the expansion
 * @param cached A cache of the property shapes already expanded
 */
// function expand(shape: sh.PropertyShape, nested = true, cached = {}) {
//   if (shape.path.term.termType === 'NamedNode') {
//     return shape;
//   }
//   if (shape.path.term.termType === 'BlankNode') {
//     shape.alternativePath.
//   }
//   throw new Error('Expected path to be either BlankNode or NamedNode');
// }

// Logic of this module
// sh:path ( a b c d )
// minCount x; maxCount y;
